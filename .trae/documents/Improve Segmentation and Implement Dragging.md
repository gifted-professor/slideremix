我将为您实现 **更好的图像分割** 和 **文本拖拽定位** 功能。

### 1. 优化图像分割 (Gemini Service)
我将更新 `geminiService.ts` 中的 AI 提示词 (Prompt)，指令 Gemini：
*   **拆解复杂图表**：明确要求将复杂的图表（如“AI 大脑”或“循环图”）拆解为更小的独立组件（图标、箭头、形状），而不是将它们合并为一个大图片。
*   **分离文本**：强制要求将文本标签与背景图像/形状严格分离，确保文本保持可编辑状态。
*   **精细化检测**：鼓励识别更小的视觉元素（Logo、图标）作为独立的 `raster_image` 或 `vector_shape`。

### 2. 实现文本拖拽与定位
我将更新 **选中覆盖层 (Selection Overlay)** 以支持拖拽交互：
*   **拖拽逻辑**：在选中框上添加鼠标事件监听 (`onMouseDown`, `onMouseMove`, `onMouseUp`)。
*   **实时更新**：根据鼠标移动计算新的 `x` 和 `y` 坐标，并实时更新 `ElementSettings`。
*   **视觉反馈**：确保元素跟随鼠标流畅移动。

### 3. 组件整合与恢复
我将确保所有必要的组件（`ElementToolbar`, `SelectionOverlay`）都已正确实现并集成到 `App.tsx` 中，以支持上述交互功能。

### 4. 验证
*   我将验证拖拽文本元素是否能正确更新其在画布上的位置。
*   （需用户验证）：您需要重新上传图片，以测试新的提示词是否改善了图表元素的“切割”效果。
